//Generate automaticaly by dbuilder
import React from "react";
import { Box } from "@billers/library";

const Footer = (props) => {
  return <Box {...props["ROOT"]} />;
};

export default Footer;

Footer.defaultProps = {
  ROOT: { tag: "footer" },
};
